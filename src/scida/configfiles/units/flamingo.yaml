metadata_unitsystem: cgs
units:
  # h: 0.7 hubble factor, needs to be set by snapshot
  # a: 1.0 # scale factor, needs to be set by snapshot
  ckpc: a * kpc
fields:
  Cells: "no_units"
  Header: "no_units"
  SubgridScheme: "no_units"
