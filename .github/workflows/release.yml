# based on https://github.com/cjolowicz/cookiecutter-hypermodern-python, MIT License, Copyright © 2020 Claudio Jolowicz
# based on https://github.com/woltapp/wolt-python-package-cookiecutter, MIT License, Copyright (c) 2021 Wolt Enterprises

name: Release

on:
  release:
    types: [ published ]

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.11

      - name: Check if there is a parent commit
        id: check-parent-commit
        run: |
          echo "sha=$(git rev-parse --verify --quiet HEAD^)" >> $GITHUB_OUTPUT

      - name: Build package
        run: |
          uv build

      - name: Publish package on PyPI
        uses: pypa/gh-action-pypi-publish@v1.13.0
        with:
          user: __token__
          password: "${{ secrets.PYPI_TOKEN }}"
